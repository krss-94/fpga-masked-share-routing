# M2/M2b results — provenance gap

`src/m2b_targeted_reroute/milestone2b_targeted_reroute.py` (the custom weighted-A*
pathfinder on the raw PIP graph) is present in the uploaded files. Its output
artifact — the README's reproduction steps name it `targeted_reroute.dcp` — was
**not found** anywhere in the uploaded archive or the file inventory.

This is disclosed here rather than fabricated. If the checkpoint exists elsewhere,
add it to this directory; otherwise the README's "0/9 nets rerouted" M2/M2b claim
currently rests on the script + the DECISION_LOG/README narrative, not a preserved
DCP in this upload.
